server {
  listen 80;
  server_name _;

  # Block inline scripts: should trigger CSP violations.
  add_header Content-Security-Policy "default-src 'self'; script-src 'self';" always;

  location / {
    root /usr/share/nginx/html;
    index index.html;
  }
}
