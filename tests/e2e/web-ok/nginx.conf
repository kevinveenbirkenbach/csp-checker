server {
  listen 80;
  server_name _;

  # Allow inline scripts, so we should NOT see CSP violations.
  add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline';" always;

  location / {
    root /usr/share/nginx/html;
    index index.html;
  }
}
