server {
  listen 80;
  server_name _;

  # Allow inline scripts to keep it "clean" (no CSP violations expected)
  add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline';" always;

  location / {
    root /usr/share/nginx/html;
    index index.html;
  }
}
