server {
  listen 80;
  server_name _;

  location / {
    add_header Content-Disposition "attachment; filename=report.bin" always;
    default_type application/octet-stream;
    return 200 "not a document";
  }
}
