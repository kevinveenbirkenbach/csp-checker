server {
  listen 80;
  server_name _;

  add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline';" always;

  root /usr/share/nginx/html;

  location = / {
    try_files /index.html =404;
  }
  location = /landed {
    try_files /landed.html =404;
  }
}
